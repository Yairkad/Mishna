# תוכנית פיתוח — אפליקציית לימוד משניות

מבוסס על `SPEC.md`.

---

## 1. ארכיטקטורה

| שכבה | טכנולוגיה |
|---|---|
| UI | Jetpack Compose, Material 3, RTL |
| ניווט | Navigation Compose (3 טאבים) |
| נתונים | Room (תוכן + נתוני משתמש), DataStore (הגדרות) |
| DI | Hilt |
| זמנים ולוח עברי | KosherJava Zmanim (זריחה, תאריך עברי, חגים א"י) |
| התראות | AlarmManager + BroadcastReceiver (כולל BOOT_COMPLETED) |
| גיבוי שבועי | WorkManager + Storage Access Framework (Drive בהמשך) |
| בדיקות | JUnit לליבה הלוגית, Compose UI tests למסכים עיקריים |

מבנה מודולים (מודול אחד, חבילות נפרדות):
```
data/content   — מאגר המשנה והפירושים (read-only, מגיע עם ה-APK)
data/user      — היסטוריה, הקצאות, הגדרות
domain         — חישוב יום לימוד, הקצאה, רצף, סטטיסטיקות, התראות
ui/onboarding, ui/study, ui/history, ui/settings
```

---

## 2. מודל נתונים

**תוכן (DB ארוז מראש):**
- `Mishna(globalIndex, seder, masechet, perek, mishna, text)` — `globalIndex` רץ 0..~4190 לפי סדר הלימוד, כך שהקצאה = טווח אינדקסים.
- `Commentary(globalIndex, source, text)` — source: BARTENURA / IKAR_TYT.

**משתמש:**
- `StudyDay(date, startIndex, endIndex, completedAt?)` — שורה לכל יום לימוד.
- `PlanState(nextIndex, pace, paceChangedFrom)`
- הגדרות ב-DataStore.

**לוגיקת ליבה (פונקציות טהורות, עם בדיקות):**
- `currentStudyDate(now, location)` — לפני הזריחה → אתמול.
- `assignmentFor(date)` — מ-`nextIndex`, `pace` משניות, חוצה מסכתות/סדרים.
- `streak(days)` — רצף נוכחי ושיא.
- `estimatedFinish(nextIndex, pace)`.
- `backfill(startDate, pace, today, unmarkedDays)` — חישוב הקצאות לימי עבר.
- `shiftDay(direction)` — הזזת נקודת הלימוד ביום קדימה/אחורה.
- `notificationSchedule(date)` — לפי סוג היום (חול/ערב/קודש).

---

## 3. הכנת התוכן
1. סקריפט (Python) שמוריד מ-Sefaria API: משנה מנוקדת, ברטנורא, עיקר תוספות יום טוב.
2. ניקוי HTML, מיפוי פירוש ↔ משנה.
3. בניית `mishna.db` מוכן, שמוכנס ל-`assets` ונטען ב-Room (`createFromAsset`).
4. בדיקת רישוי לכל טקסט (מוצג במסך "אודות").

---

## 4. שלבי פיתוח

| שלב | תוכן | תוצר |
|---|---|---|
| 0 | שלד פרויקט, Gradle, Hilt, Room, CI בסיסי | אפליקציה ריקה שנבנית |
| 1 | סקריפט תוכן + DB ארוז | מאגר מלא בתוך ה-APK |
| 2 | ליבה: יום לימוד, הקצאה, רצף, סטטיסטיקות + בדיקות | לוגיקה מכוסה בבדיקות |
| 3 | Onboarding | הגדרה ראשונית עובדת |
| 4 | מסך לימוד: דפים, פירושים, שמירת מיקום, כפתור סיום | **MVP שמיש** |
| 5 | היסטוריה: לוח שנה, רצפים, סטטיסטיקות | |
| 6 | התראות לפי סוג יום | |
| 7 | הגדרות (רגילות + מתקדמות) | |
| 8 | גיבוי: ייצוא/ייבוא JSON, גיבוי שבועי לקובץ מקומי | |
| 9 | ליטוש, מצב כהה, בדיקות על מכשיר, APK חתום | גרסה 1.0 |
| 10 | גיבוי ל-Google Drive | גרסה 1.1 |

---

## 5. החלטות שהתקבלו בבדיקת פרצות
כל ההחלטות שולבו ב-`SPEC.md`:
- **שבת וחג:** סימון במוצאי היום הקדוש, בלי גלילה. תצוגה מראש בערב שבת/חג.
- **סיום ששת הסדרים:** מסך סיום ומחזור חדש.
- **"יום 45":** ימי לוח מתאריך ההתחלה.
- **תאריך התחלה בעבר:** חישוב לאחור, ימים מסומנים כבוצעו עם אפשרות לבטל. כפתור יום קדימה/אחורה.
- **שינוי נקודת התחלה:** ההיסטוריה נשמרת.
- **גיבוי:** קובץ מקומי שבועי ב-1.0, Drive בהמשך.
- **כפתור סיום:** גלילה רק עד סוף מה שמוצג.
- **שינוי אזור זמן:** חל מהזריחה הבאה.
- **הרשאות:** התראות ותזמון מדויק ב-Onboarding.
