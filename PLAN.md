# תוכנית פיתוח — אפליקציית לימוד משניות

מבוסס על `SPEC.md`.

---

## 1. ארכיטקטורה

| שכבה | טכנולוגיה |
|---|---|
| UI | Jetpack Compose, Material 3, RTL |
| ניווט | Navigation Compose (3 טאבים) |
| נתונים | Room (תוכן + נתוני משתמש), DataStore (הגדרות) |
| DI | Hilt |
| זמנים ולוח עברי | KosherJava Zmanim (זריחה, תאריך עברי, חגים א"י) |
| התראות | AlarmManager + BroadcastReceiver (כולל BOOT_COMPLETED) |
| גיבוי שבועי | WorkManager + Google Drive API (appDataFolder) |
| בדיקות | JUnit לליבה הלוגית, Compose UI tests למסכים עיקריים |

מבנה מודולים (מודול אחד, חבילות נפרדות):
```
data/content   — מאגר המשנה והפירושים (read-only, מגיע עם ה-APK)
data/user      — היסטוריה, הקצאות, הגדרות
domain         — חישוב יום לימוד, הקצאה, רצף, סטטיסטיקות, התראות
ui/onboarding, ui/study, ui/history, ui/settings
```

---

## 2. מודל נתונים

**תוכן (DB ארוז מראש):**
- `Mishna(globalIndex, seder, masechet, perek, mishna, text)` — `globalIndex` רץ 0..~4190 לפי סדר הלימוד, כך שהקצאה = טווח אינדקסים.
- `Commentary(globalIndex, source, text)` — source: BARTENURA / IKAR_TYT.

**משתמש:**
- `StudyDay(date, startIndex, endIndex, completedAt?)` — שורה לכל יום לימוד.
- `PlanState(nextIndex, pace, paceChangedFrom)`
- הגדרות ב-DataStore.

**לוגיקת ליבה (פונקציות טהורות, עם בדיקות):**
- `currentStudyDate(now, location)` — לפני הזריחה → אתמול.
- `assignmentFor(date)` — מ-`nextIndex`, `pace` משניות, חוצה מסכתות/סדרים.
- `streak(days)` — רצף נוכחי ושיא.
- `estimatedFinish(nextIndex, pace)`.
- `notificationSchedule(date)` — לפי סוג היום (חול/ערב/קודש).

---

## 3. הכנת התוכן
1. סקריפט (Python) שמוריד מ-Sefaria API: משנה מנוקדת, ברטנורא, עיקר תוספות יום טוב.
2. ניקוי HTML, מיפוי פירוש ↔ משנה.
3. בניית `mishna.db` מוכן, שמוכנס ל-`assets` ונטען ב-Room (`createFromAsset`).
4. בדיקת רישוי לכל טקסט (מוצג במסך "אודות").

---

## 4. שלבי פיתוח

| שלב | תוכן | תוצר |
|---|---|---|
| 0 | שלד פרויקט, Gradle, Hilt, Room, CI בסיסי | אפליקציה ריקה שנבנית |
| 1 | סקריפט תוכן + DB ארוז | מאגר מלא בתוך ה-APK |
| 2 | ליבה: יום לימוד, הקצאה, רצף, סטטיסטיקות + בדיקות | לוגיקה מכוסה בבדיקות |
| 3 | Onboarding | הגדרה ראשונית עובדת |
| 4 | מסך לימוד: דפים, פירושים, שמירת מיקום, כפתור סיום | **MVP שמיש** |
| 5 | היסטוריה: לוח שנה, רצפים, סטטיסטיקות | |
| 6 | התראות לפי סוג יום | |
| 7 | הגדרות (רגילות + מתקדמות) | |
| 8 | גיבוי: ייצוא/ייבוא JSON, גיבוי שבועי ל-Drive | |
| 9 | ליטוש, מצב כהה, בדיקות על מכשיר, APK חתום | גרסה 1.0 |

---

## 5. פרצות בתכנון (דורשות החלטה)

### א. שבת וחג — הרצף נשבר (קריטי)
יום לימוד מסתיים בזריחה הבאה, וסימון בדיעבד אסור. לכן:
- **יום טוב שחל ביום שישי, או חג צמוד לשבת** (למשל ראש השנה): היום הראשון מסתיים בזריחה של היום השני, שגם בו לא משתמשים בטלפון. אין אפשרות לסמן אותו, והרצף נשבר בכל פעם.
- גם בשבת רגילה: אם לומדים מתוך ספר, כפתור הסיום (שדורש גלילה באפליקציה) לא משקף את הלימוד בפועל.

**הצעה:** ימי שבת וחג מסומנים במוצאי היום הקדוש האחרון ברצף, בלי דרישת גלילה ("למדתי מהספר"). בערב שבת/חג האפליקציה מציגה מראש מה ללמוד בכל יום.

### ב. סיום ששת הסדרים
לא מוגדר מה קורה אחרי המשנה האחרונה.
**הצעה:** מסך סיום (סיום הש"ס משנה) ואפשרות להתחיל מחזור חדש. ההיסטוריה נשמרת.

### ג. "יום 45" — מה סופרים?
ימים קלנדריים מתחילת התוכנית, או ימים שהושלמו?
**הצעה:** ימים קלנדריים (כך המספר מתקדם גם ביום שהוחמץ).

### ד. תאריך התחלה בעבר
אם ב-Onboarding בוחרים תאריך שעבר, מה קורה לימים שביניהם?
**הצעה:** לא לאפשר תאריך בעבר, רק היום או בעתיד.

### ה. שינוי נקודת התחלה כשיש היסטוריה
**הצעה:** ההיסטוריה והרצף נשמרים, והלימוד ממשיך מהנקודה החדשה.

### ו. גיבוי אוטומטי ל-Drive
דורש התחברות לחשבון Google והגדרת פרויקט ב-Google Cloud (OAuth עם טביעת האצבע של חתימת ה-APK). זו עבודה לא מבוטלת לאפליקציה אישית.
**הצעה:** בשלב ראשון גיבוי שבועי **לקובץ מקומי** + גיבוי Android המובנה. Drive בשלב מאוחר יותר.

### ז. כפתור הסיום כשהפירושים מקופלים
אם כל הפירושים של המשנה האחרונה מקופלים, מספיק לגלול עד סוף המשנה.
**הצעה:** כן, הדרישה חלה רק על מה שמוצג.

### ח. טיסה / שינוי אזור זמן
**הצעה:** יום לימוד שכבר התחיל לא משתנה. המיקום החדש חל מהזריחה הבאה.

### ט. הרשאות Android חדשות
Android 12 ומעלה דורש הרשאה לתזמון התראות מדויקות, ו-Android 13 ומעלה דורש הרשאה להצגת התראות. צריך לבקש אותן ב-Onboarding.
